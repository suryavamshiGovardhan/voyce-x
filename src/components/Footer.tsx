import SiteFooter from "@/components/site/SiteFooter";

/**
 * Legacy footer entry point. Kept so existing pages keep working,
 * but it now renders the single unified VOYCE footer.
 */
export default function Footer() {
  return <SiteFooter />;
}
